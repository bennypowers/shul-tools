import type { ReactiveController, ReactiveControllerHost } from "lit";
export declare class ClockController<T extends ReactiveControllerHost> implements ReactiveController {
    #private;
    get date(): Date;
    constructor(host: T, key?: keyof T);
    set(date: Date): void;
    reset(): void;
    start(): void;
    stop(): void;
    hostConnected(): void;
    hostDisconnected(): void;
}
