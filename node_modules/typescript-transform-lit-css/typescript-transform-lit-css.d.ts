import type { Options } from '@pwrs/lit-css/lit-css';
import type { TransformerExtras, PluginConfig } from 'ts-patch';
import ts, { SourceFile } from 'typescript';
export interface LitCSSOptions extends Pick<Options, 'specifier' | 'filePath' | 'tag'> {
    filter: string;
    uglify: boolean;
    inline: boolean;
}
/**
 * Replace .css import specifiers with .css.js import specifiers
 */
export default function (program: ts.Program, pluginConfig: PluginConfig & LitCSSOptions, extras: TransformerExtras): (ctx: ts.TransformationContext) => (sourceFile: SourceFile) => ts.SourceFile;
